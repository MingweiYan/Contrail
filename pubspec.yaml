name: contrail
description: "Contrail is a productivity app that helps you focus on your tasks and achieve your goals."
publish_to: 'none'

version: 1.0.1+2

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  logger: ^2.0.2+1
  get_it: ^7.6.7
  go_router: ^14.1.0
  image_picker: ^1.1.1
  provider: ^6.0.5
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  fl_chart: ^0.68.0
  flutter_local_notifications: ^17.0.0
  flutter_background_service: ^5.0.0
  shared_preferences: ^2.2.2
  uuid: ^4.4.0
  intl: ^0.20.2
  file_picker: ^8.0.0
  cupertino_icons: ^1.0.8
  timezone: ^0.9.2
  flutter_colorpicker: ^1.1.0
  flutter_quill: ^11.4.2
  wakelock_plus: ^1.1.4
  flutter_screenutil: ^5.9.0
  permission_handler: ^12.0.1
  path_provider: ^2.1.2
  device_info_plus: ^9.1.2
  flutter_svg: ^2.0.9
  saf: ^1.0.0
  open_filex: ^4.3.2


dev_dependencies:
  flutter_test:
    sdk: flutter

  hive_generator: ^1.1.0
  build_runner: ^2.4.6
  mocktail: ^1.0.3
  flutter_lints: ^5.0.0
  dart_code_metrics: ^4.19.2

flutter:
  uses-material-design: true
  # 已移除不存在的资源声明，因为这些文件实际上不存在于项目中
  
  # 应用资源配置
  assets:
    - assets/icons/
    - assets/images/
  
  # 应用图标配置
  # Android应用图标
  # 注意：对于Android的自适应图标，我们需要在android/app/src/main/res/目录下配置
  # 这里我们简单配置基础图标
  # icon: assets/icons/icon.png
  # iOS应用图标已在Xcode中配置
