# 统计聚合性能说明

- 聚合管线：先按本地 dateOnly 进行分组，构建 Map<dateOnly, totalSeconds>，再在周/月视图按范围读取，年视图按月份匹配。
- 复杂度：分组阶段 O(N)（N 为 trackingDurations 键数量）；读取阶段 O(R)（R 为目标范围内的天数/周数/月份），总体优于各分支重复扫描。
- 单位：内部以秒累加，输出为分钟（double），避免短时记录被整取为 0。
- 兼容性：不更改写入端键与数据结构；支持含时分秒键与 dateOnly 键混合存在。
